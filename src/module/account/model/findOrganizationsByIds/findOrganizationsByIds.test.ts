import { describe, it } from "node:test"

describe("findOrganizationsByIds", () => {
  it("レコードの存在するIDを複数指定するとヒットする組織情報を全て返すこと", (t) => {
    t.todo()
  })

  it("レコードの存在しないIDを指定すると空配列を返すこと", (t) => {
    t.todo()
  })
})
