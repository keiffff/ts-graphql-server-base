import { describe, it } from "node:test"

describe("findAccountById", () => {
  it("レコードの存在するIDを指定するとアカウント情報を返すこと", (t) => {
    t.todo()
  })

  it("レコードの存在しないIDを指定するとnullを返すこと", (t) => {
    t.todo()
  })
})
